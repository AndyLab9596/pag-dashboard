import styled from '@emotion/styled';

const Wrapper = styled.div`
  .card {
    width: 31%;

    margin-left: 16px;
    font-size: 17px;
    color: #3f536e;

    &:hover {
      cursor: auto;
    }
  }
`;

export default Wrapper;
